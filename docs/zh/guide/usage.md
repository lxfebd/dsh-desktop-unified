# 使用

## 1. 打开应用

安装后启动 **DSH Desktop**。它会启动本地 web 服务并打开原生窗口。

## 2. 填入 API Key

在应用设置里粘贴你的 [DeepSeek API Key](https://platform.deepseek.com/)——和网页版操作一样。

## 3. 开始对话

让 AI 帮你跑任务：读写文件、执行命令、写代码、自动化操作。

## 数据存在哪

对话、配置和会话存在系统应用数据目录里，绝不污染你的用户目录。诊断日志写在同目录的 `logs/dsh.log`。

如果应用打不开或界面白屏，先看这个日志文件——托盘菜单的「打开日志」和「打开数据目录」可以直接到达这两个位置。
