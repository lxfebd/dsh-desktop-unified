# dsh-terminal bundle patch
# 只一行 host entry（注册 /api/terminal/* 路由）。
# client.js 的浏览器加载由 package.json 的 dsh.client 字段触发，
# 不需要在 yml 里写单独的 client 行 —— 那会导致同一包被 mount 两次。
- insert:
    - id: dsh-terminal
      name: dsh-terminal
      inject: [webServer]
