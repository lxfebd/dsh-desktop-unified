node_modules/
dist/
dist-installer/
*.log
.DS_Store

# Local pnpm content-addressable store + npm cache (this machine's G: is dead)
.pnpm-store/
.npm-cache/

# ZCode agent local state (session plans, etc.)
.zcode/

# VitePress build output
docs/.vitepress/dist/
docs/.vitepress/cache/

# Local agent-generated working docs (engineering plans, specs, handover
# ledger, progress board) — kept on this machine only, never uploaded.
# p6-plugins-contribution.json stays tracked: it is the upstream contribution
# draft, not agent-generated working material.
docs/handover/HANDOVER.md
docs/handover/progress-board.html
docs/handover/superpowers/

# Local agent scratch scripts (polling/release verification)
tmp/
